# Gravity Volume Control 🪨🔊

[![Platform](https://img.shields.io/badge/platform-macOS%2013.0+-blue.svg)](https://www.apple.com/macos/)
[![Swift](https://img.shields.io/badge/Swift-6.0-orange.svg)](https://swift.org/)
[![Tests](https://img.shields.io/badge/tests-48%20passed-brightgreen.svg)](#тестирование)
[![License](https://img.shields.io/badge/license-MIT-lightgrey.svg)](../../LICENSE)

**🌍 [English](../../README.md) | [日本語](README_ja.md) | [简体中文](README_zh-CN.md) | [繁體中文](README_zh-TW.md) | [فارسی](README_fa.md) | [العربية](README_ar.md)**

**Превратите трекпад вашего MacBook в «Самый неудобный регулятор громкости в мире»**

[Gravity Volume Control] — это шуточное приложение, которое использует встроенный в трекпад MacBook датчик давления Force Touch для **управления громкостью системы с помощью веса предметов, размещённых на нём**.

Разработано как дань уважения популярному в инженерном сообществе конкурсу «Худший интерфейс регулировки громкости».

https://github.com/user-attachments/assets/demo-video-placeholder

---

## Содержание

- [Как использовать](#как-использовать-the-bad-experience)
- [Требования](#требования)
- [Установка](#установка)
- [Разработка](#разработка)
- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Возможности для обучения](#возможности-для-обучения-for-engineers)
- [Отказ от ответственности](#отказ-от-ответственности)

---

## Как использовать (The "Bad" Experience)

1. Запустите приложение.
2. Положите палец на трекпад (необходимо для электропроводности).
3. Положите рядом с пальцем **«груз»**.
   - 🪶 **Лёгкие предметы (например, ластик)** → 🔈 Громкость: низкая
   - 🍺 **Тяжёлые предметы (например, полная кружка)** → 🔊 Громкость: высокая
4. **Держите тяжёлый предмет на трекпаде всё время**, пока смотрите видео. Если уберёте его, звук мгновенно отключится.

---

## Требования

| Пункт | Требование |
|-------|------------|
| ОС | macOS 13.0 (Ventura) или новее |
| Оборудование | MacBook с трекпадом Force Touch |
| Xcode | 16.0 или новее |
| Swift | 6.0 |

> **Примечание:** НЕ работает с внешними трекпадами или Magic Trackpad. Поддерживается только встроенный трекпад MacBook.

---

## Установка

### Homebrew (Рекомендуется)

```bash
brew install --cask gravity-volume-control
```

### Ручная установка

1. Скачайте последний `.dmg` из [Releases](https://github.com/clearclown/volumeControlWithTrackWeight/releases)
2. Перетащите `Gravity Volume Control.app` в `/Applications`
3. При первом запуске, если появится сообщение «Разработчик не может быть проверен», перейдите в Системные настройки > Конфиденциальность и безопасность и разрешите

### Сборка из исходников

```bash
git clone https://github.com/clearclown/volumeControlWithTrackWeight.git
cd volumeControlWithTrackWeight
xcodebuild build -scheme TrackWeight -destination 'platform=macOS'
```

---

## Разработка

### Философия разработки

Этот проект следует принципам **TDD (Test-Driven Development)**.

- При добавлении новых функций сначала пишутся тесты
- Успех тестов приоритетнее ручного тестирования на устройстве
- ViewModel используют внедрение зависимостей (DI) для тестируемого дизайна

### Сборка

```bash
# Сборка приложения
xcodebuild build -scheme TrackWeight -destination 'platform=macOS' \
  CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO
```

### Тестирование

```bash
# Запуск всех тестов
xcodebuild test -scheme TrackWeight -destination 'platform=macOS' \
  -only-testing:TrackWeightTests \
  CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO
```

**Покрытие тестами:**

| Набор тестов | Количество тестов | Цель |
|--------------|-------------------|------|
| `ScaleViewModelTests` | 17 | Базовая функциональность весов |
| `WeighingViewModelTests` | 24 | Рабочий процесс взвешивания и переходы состояний |
| `WeighingStateTests` | 7 | Равенство состояний |
| **Итого** | **48** | - |

### Поддержка разработки с ИИ

При разработке с помощью ИИ-ассистентов (Claude Code и др.) обращайтесь к `CLAUDE.md` для ознакомления с правилами TDD и рекомендациями.

---

## Архитектура

Это приложение использует паттерн **MVVM (Model-View-ViewModel)** для реактивного отражения аппаратного ввода в UI и системные настройки.

```mermaid
graph TD
    Hardware[Датчики давления трекпада] -->|Сырые данные давления| OMS[Фреймворк OpenMultitouchSupport]
    OMS -->|Асинхронный поток| VM[ScaleViewModel / WeighingViewModel]

    subgraph "Логика приложения"
        VM -->|Обработка сигнала| Logic[Сглаживание и отображение]
        Logic -->|Вычисленная громкость| PublishedVar[@Published volume]
    end

    PublishedVar -->|Привязка| View[SwiftUI View]
    PublishedVar -->|Побочный эффект| AudioAPI[Системное аудио macOS]
```

### Поток данных

1. **Источник данных:** `OpenMultitouchSupport` перехватывает аппаратные прерывания и генерирует события касания.
2. **ViewModel:** `ScaleViewModel` получает данные через асинхронный поток (`for await`), выполняет шумоподавление и преобразование значений (давление 0.0~1.0 → громкость 0.0~100.0).
3. **View:** SwiftUI обнаруживает изменения свойств `@Published` и отрисовывает «тяжело выглядящие анимации».
4. **Система:** Одновременно обновляет системную громкость в фоне.

### Внедрение зависимостей (DI)

ViewModel спроектированы для тестируемости:

```swift
// Продакшен (по умолчанию)
let viewModel = ScaleViewModel() // Использует OMSManager.shared

// Тестирование
let mockProvider = MockTouchDataProvider()
let viewModel = ScaleViewModel(touchProvider: mockProvider)
```

---

## Структура проекта

```
volumeControlWithTrackWeight/
├── TrackWeight/                    # Основное приложение
│   ├── TrackWeightApp.swift        # Точка входа
│   ├── ContentView.swift           # Главный экран
│   ├── ScaleView.swift             # UI весов
│   ├── ScaleViewModel.swift        # Логика весов
│   ├── WeighingView.swift          # UI взвешивания
│   ├── WeighingViewModel.swift     # Рабочий процесс взвешивания
│   ├── WeighingState.swift         # Определения состояний
│   └── TouchDataProviding.swift    # Протокол DI
│
├── TrackWeightTests/               # Модульные тесты
│   ├── Mocks/
│   │   └── MockTouchDataProvider.swift
│   ├── ScaleViewModelTests.swift
│   ├── WeighingViewModelTests.swift
│   └── WeighingStateTests.swift
│
├── CLAUDE.md                       # Руководство по разработке с ИИ
└── README.md
```

---

## Возможности для обучения (For Engineers)

На первый взгляд это просто шуточное приложение, но оно содержит продвинутые технические задачи. Как Swift-инженер, вы можете практически изучить следующее:

### 1. Приватные API Apple и управление оборудованием

Узнайте, как анализировать и использовать приватный фреймворк `OpenMultitouchSupport` для доступа к обычно недоступным сырым данным трекпада (давление, площадь контакта).

- **Ключевые файлы:** `ContentViewModel.swift`, `TouchDataProviding.swift`

### 2. Современная конкурентность Swift (Async/Await)

Эффективная обработка непрерывных потоков данных от сенсоров с использованием новейшей модели конкурентности Swift 6.0.

- Мониторинг событий с помощью `AsyncStream`
- Безопасная привязка данных к UI-потоку с помощью `Task` и `@MainActor`
- **Ключевые файлы:** `ScaleViewModel.swift`, `WeighingViewModel.swift`

### 3. Обработка сигналов

Сырые значения сенсоров постоянно колеблются (шум). Включает реализации алгоритмов для преобразования этого в плавные, комфортные изменения громкости.

| Параметр | Значение | Описание |
|----------|----------|----------|
| `historySize` | 10 | Размер окна скользящего среднего |
| `rateOfChangeThreshold` | 5.0 | Порог скорости обнаружения объекта |
| `stabilityThreshold` | 2.0 | Допуск для определения стабильности |
| `fingerHoldDuration` | 3.0с | Время ожидания обнаружения пальца |
| `stabilityDuration` | 3.0с | Время ожидания стабильности |

- **Ключевые файлы:** `WeighingViewModel.swift`

### 4. Интеграция с системой macOS

Узнайте об интеграции с `AudioToolbox` и `Core Audio` для управления общесистемной громкостью, а также об управлении разрешениями в песочнице (`entitlements`).

### 5. Тестируемая архитектура

Изучите паттерны проектирования, которые позволяют проводить модульное тестирование даже для кода, зависящего от приватных API, через абстракцию протоколов и внедрение зависимостей.

```swift
protocol TouchDataProviding: AnyObject, Sendable {
    var touchDataStream: AsyncStream<[OMSTouchData]> { get }
    @MainActor func startListening() -> Bool
    @MainActor func stopListening() -> Bool
}
```

---

## Вклад в проект

1. Сделайте форк этого репозитория
2. Создайте ветку функции (`git checkout -b feature/amazing-feature`)
3. **Сначала напишите тесты**, затем реализуйте
4. Убедитесь, что все тесты проходят (`xcodebuild test ...`)
5. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
6. Отправьте ветку (`git push origin feature/amazing-feature`)
7. Создайте Pull Request

---

## Отказ от ответственности

> **Предупреждение:** Не кладите на трекпад чрезмерно тяжёлые или острые предметы. Это может повредить ваш трекпад. Разработчик не несёт ответственности за любые повреждения оборудования, вызванные использованием этого приложения.

**Это шутка. Пожалуйста, не используйте его в повседневной жизни.**

---

## Лицензия

MIT License — подробности см. в [LICENSE](../../LICENSE).
